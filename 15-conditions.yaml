- name: gathering facts
  hosts: local
  connection: local
  tasks:
  - name: print ansible facts
    ansible.builtin.debug:
      msg: "{{ ansible_facts }}"
  
  - name: check redhat or not
    ansible.builtin.debug:
      msg: "Yes, it is Redhat"
    when: ansible_facts.os_family == "Redhat"